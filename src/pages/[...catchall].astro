---
// フォールバックページ
// このページは存在しないパスへのアクセスをキャッチします

// リクエストURLを取得
const url = new URL(Astro.request.url);
const path = url.pathname;

// ログ出力（デバッグ用）
console.log(`404エラー: パス "${path}" が見つかりません`);

// ホームページにリダイレクト
return Astro.redirect('/');
---

<script>
  // クライアントサイドでのリダイレクト（バックアップ）
  window.location.href = '/';
</script> 